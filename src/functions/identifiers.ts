import { View } from '@nativescript/core'

export function getViewsByIdentifiers (...idNames: string[]): View[] {
  const views: View[] = []

  console.log('#### GET VIEWS BY IDENTIFIERS ####', idNames)

  return views
}
