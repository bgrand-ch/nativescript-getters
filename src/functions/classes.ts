import { View } from '@nativescript/core'

export function getViewsByClasses (...classNames: string[]): View[] {
  const views: View[] = []

  console.log('#### GET VIEWS BY CLASS ####', classNames)

  return views
}
