import { View } from '@nativescript/core'

export function getViewsByTags (...tagNames: string[]): View[] {
  const views: View[] = []

  console.log('#### GET VIEWS BY TAGS ####', tagNames)

  return views
}
